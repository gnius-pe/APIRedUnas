package com.redunas.redunas.repositorio;

import com.redunas.redunas.modelo.DTO.UsuarioPublicacion;
import org.springframework.data.jpa.repository.JpaRepository;

public interface UsuarioPublicacionRepositorio extends JpaRepository<UsuarioPublicacion,Long> {
}
